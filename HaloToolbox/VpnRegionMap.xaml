<UserControl x:Class="HaloToolbox.VpnRegionMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             Focusable="True"
             FontFamily="Consolas"
             Foreground="{DynamicResource TextBrush}">
    <UserControl.Resources>
        <Style x:Key="MapToolButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="MinWidth" Value="30"/>
        </Style>
        <Style x:Key="MapRegionButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Width="16" Height="16"
                                     Fill="{DynamicResource SurfaceBrush}"/>
                            <Ellipse x:Name="Glow" Width="22" Height="22"
                                     Fill="{DynamicResource AccentBrush}" Opacity="0"/>
                            <Ellipse x:Name="Dot" Width="10" Height="10"
                                     Fill="{DynamicResource AccentBrush}"
                                     Stroke="{DynamicResource SurfaceBrush}" StrokeThickness="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Glow" Property="Opacity" Value="0.14"/>
                                <Setter TargetName="Dot" Property="Width" Value="14"/>
                                <Setter TargetName="Dot" Property="Height" Value="14"/>
                                <Setter TargetName="Dot" Property="Fill" Value="{DynamicResource SurfaceBrush}"/>
                                <Setter TargetName="Dot" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Dot" Property="StrokeThickness" Value="3"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Selected">
                                <Setter TargetName="Glow" Property="Opacity" Value="0.18"/>
                                <Setter TargetName="Dot" Property="Width" Value="16"/>
                                <Setter TargetName="Dot" Property="Height" Value="16"/>
                                <Setter TargetName="Dot" Property="Fill" Value="{DynamicResource SurfaceBrush}"/>
                                <Setter TargetName="Dot" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Dot" Property="StrokeThickness" Value="3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="Root" ClipToBounds="True" Background="{DynamicResource SurfaceBrush}">
        <Canvas x:Name="Viewport"
                Background="Transparent"
                ClipToBounds="True"
                Cursor="Arrow"
                SizeChanged="Viewport_SizeChanged"
                MouseWheel="Viewport_MouseWheel"
                MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                MouseMove="Viewport_MouseMove"
                MouseLeave="Viewport_MouseLeave">
            <Canvas x:Name="WorldCanvas" Width="1000" Height="500">
                <Canvas.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="MapScale" ScaleX="1" ScaleY="1"/>
                        <TranslateTransform x:Name="MapTranslate"/>
                    </TransformGroup>
                </Canvas.RenderTransform>
                <Canvas x:Name="GridCanvas" Width="1000" Height="500" IsHitTestVisible="False"/>
                <Canvas x:Name="LandCanvas" Width="1000" Height="500" IsHitTestVisible="False"/>
                <Canvas x:Name="RouteCanvas" Width="1000" Height="500" IsHitTestVisible="False"/>
                <Canvas x:Name="RegionCanvas" Width="1000" Height="500"/>
            </Canvas>
        </Canvas>

        <Border HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="10" Padding="5" CornerRadius="3"
                Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Button Content="−" Style="{StaticResource MapToolButton}" Click="ZoomOut_Click" Margin="0,0,5,0"/>
                <Button Content="+" Style="{StaticResource MapToolButton}" Click="ZoomIn_Click" Margin="0,0,5,0"/>
                <Button x:Name="BtnMapView" Content="WORLD" Style="{StaticResource MapToolButton}" Click="MapView_Click"/>
            </StackPanel>
        </Border>

        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom"
                Margin="10" Padding="8,5" CornerRadius="3"
                Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                IsHitTestVisible="False">
            <TextBlock FontSize="9" Foreground="{DynamicResource MutedBrush}">
                <Run Text="SCROLL TO ZOOM" Foreground="{DynamicResource TextBrush}"/>
                <Run Text="  ·  DRAG TO PAN  ·  CLICK A REGION"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
